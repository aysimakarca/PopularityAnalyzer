# Spotify authentication files

This folder contains the Spotify developer-app configuration and local OAuth
token caches used by the Spotify scripts:

- `spotify_config.json`
- `.spotify_cache`
- `.spotify_playlist_write_cache`

These files can contain client secrets or access/refresh tokens. They are
excluded from Git and must not be shared or printed.

